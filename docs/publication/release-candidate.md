# Concrete publication packet — 0.1.0

Status: prepared for owner authorization. No push, tag, release or native
Tanduna write has occurred. Local tests and same-host packaged runtime checks
pass; external and human evidence remain pending.

## GitHub action requested

Use the existing authenticated account `thepianistdirector` for
https://github.com/thepianistdirector/renewal-engine. Push the reviewed
`codex/renewal-engine-0.1` branch, create tag `v0.1.0` on its reviewed commit, and
publish GitHub Release `Renewal Engine 0.1.0`. Do not merge automatically.
The exact reviewed commit and aggregate diff are supplied with the approval
request; all changes are relative to founding HEAD
`f434e00913f5df6c78721300ca68625401d87a41`.

Attach only:

- `renewal-engine.pyz`: self-contained Python CLI, SHA-256
  `ee6b2b9e6ba3ffc3e34102dc70ac596b1e3f8479bacd8a6e99072a7c78c80047`.
- `renewal-engine-0.1.0.tar.gz`: full source, installer, lawful original fixture,
  test and build tools, planning/lineage, notices and retained example reports.
- `SHA256SUMS`: final CLI and source archive hashes from the final build.

The full-source archive hash is intentionally recorded alongside the artifact
in `dist/SHA256SUMS`, outside the archive, to avoid a self-referential hash.
Old development artifacts are excluded from the proposed upload.

The source archive and CLI use the repository AGPL-3.0 license. CPython license
reference material retains its own notices. No runtimes, browser binaries,
third-party application fixture, private repository input or participant data
are distributed. No package registry, hosted execution service, new account,
paid resource, global installation or credential scope expansion is requested.
GitHub receives the public code, fixtures, plan and bounded evidence. Public
copies may persist even if the release, tag or branch is later removed.

Before publication, recheck origin/remote state and compare the approved commit
and asset hashes; a changed artifact requires reconciliation, not silent
substitution. After publication, read back release and tag, anonymously download
assets, verify checksums, then reproduce in a fresh external environment.
The release notes must disclose that required human validation is pending.

## Native Tanduna publication

The canonical export is `docs/publication/tanduna-plan-export.json`: 218 tasks,
26 waves, 50 outcomes for 0.1, 72 later 0.x, 80 long-term and 16 exploratory.
All twelve frozen source identities remain mapped, with historical contracts
unmodified. This file is review material, not an asserted server import schema.

Destination project: `prj_9ef51e9826817323b774d5d3b900520f`.
Repository ID: `repo_ee03f828c0ad9fee23ad67dd857b2d12`.
Public readback destinations:
https://tanduna.com/projects/renewal-engine/roadmap and
https://tanduna.com/p/renewal-engine/tasks.

Supported Tanduna MCP is not connected to this task. Connect it through the
owner's supported local setup; no VPS login helper or copied credentials.
Refresh the live draft, published plan and proposal state. Adapt the export
through the documented draft/proposal schema, preserve all native IDs and
history, then present the exact proposal/hash and poll option for approval.
Submission alone is not approval. After acceptance, read back native counts,
order, dependencies, returned IDs, scope/status and actual public access
instructions. GitHub approval does not itself authorize a Tanduna vote.

## Review limitations

The supported recipe is deliberately narrow; a PASS is bounded case evidence.
Runtime verification used a real installed package on the same Linux VPS, not
an external public download or human study. Browser evidence is agent-driven
and excludes native zoom and assistive technology. The complete owner Goal
remains unfinished until public product, external/human evidence and native
plan publication have all been verified.
